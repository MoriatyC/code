package first_maven;

public class Main {
    public static void main(String[] args) {
        StringBuilder dp = new StringBuilder();
        StringBuilder dp2 = new StringBuilder(dp);
        String s = "'";
        char[] a = {'1'};
        int a1 = Integer.valueOf(new String(a));
        for (int i = 0, int j = arr.lengt - 1; i < j; i++, j--) {
            char tmp = arr[i];
            arr[i] = arr[j];
            arr[j] = tmp;
        }
        
    }
}